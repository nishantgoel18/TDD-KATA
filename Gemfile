source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "3.1.2"

gem 'rspec'      # for rspec-core, rspec-expectations, rspec-mocks
gem 'rspec-core' # for rspec-core only
gem 'pry'